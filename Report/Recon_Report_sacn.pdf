---

# 9) Example run_scans.sh (safe, lab-only)
```bash
#!/bin/bash
# run_scans.sh — lab automation (use only on authorized networks)

TARGET_NET="192.168.56.0/24"
TARGET_HOST="192.168.56.50"
OUTDIR="./scans"
mkdir -p $OUTDIR

echo "[*] Netdiscover (ARP)"
sudo netdiscover -r $TARGET_NET -P > $OUTDIR/netdiscover_${TARGET_NET}.txt

echo "[*] Nmap host discovery"
sudo nmap -sn $TARGET_NET -oA $OUTDIR/hostdiscovery

echo "[*] Quick top-100 ports on $TARGET_HOST"
sudo nmap -sS -Pn --top-ports 100 -oA $OUTDIR/quick_top100 $TARGET_HOST

echo "[*] Full service scan (may take time)"
sudo nmap -sV -sC -p- -T4 -oA $OUTDIR/full_service_${TARGET_HOST} $TARGET_HOST

echo "[*] Done. Check $OUTDIR"
